import React from 'react';
import AnamAvatar from './AnamAvatar';

export default function ChatbotAnam() {
  // Minimal stub: render the Anam avatar only to avoid previous compile/runtime issues.
  // Removed in trimmed build — stub to keep imports working
  return null;
}
